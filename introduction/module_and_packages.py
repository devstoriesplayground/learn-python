# import module with following command
import random 
colors = ["yellow","red","blue"]

# the shuffle() method takes a sequence, like a list, and reorganize the order of the items.
random.shuffle(colors)
print(colors)