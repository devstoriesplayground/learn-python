# replace() in Python returns a copy of the string where all occurrences of a substring are replaced with another substring.

string = "d3v stori3s"

# replace all instances of 'r' (old) with 'e' (new)
new_string = string.replace("3", "e")

print(string)
print(new_string)