# Byte array method return an array of the given byte


even_numbers = [2, 4, 6, 8]

# convert list to bytearray
byte_array = bytearray(even_numbers)
print(byte_array)

# Output: bytearray(b'\x02\x03\x05\x07')